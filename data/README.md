Place cancer-related documents in this folder.

Supported file types:

- PDF
- DOCX
- TXT

After adding files, run:

```bash
npm.cmd run ingest:documents
```

The app will register the files into the local document backend so they appear on the Documents page.
